diff -puw ritchielawrence-cmdow-1bbcd2b/args.cpp cmdow-1.4.8-43/args.cpp
--- ritchielawrence-cmdow-1bbcd2b/args.cpp	2016-10-23 22:54:49.000000000 +0200
+++ cmdow-1.4.8-43/args.cpp	2022-03-02 10:24:53.230537500 +0100
@@ -39,42 +39,45 @@ void ParseArgs(int argc, char *argv[], s
 	// grab every arg and push onto task queue/set options
 	//
 	for(i=1;i<argc;++i) {
-		if(!lstrcmpi("/?", argv[i])) {
+            if ((*argv[i] == '-') || (*argv[i] == '/')) {
+		if(!lstrcmpi("?", argv[i] + 1) ||
+                   !lstrcmpi("H", argv[i] + 1)) {
 			//
 			// see if user wants help on particular command
 			//
+			a->switchar = *argv[i];
 			if(argc != 2) LoadString(&a->helpcmd, argv[++i]);
 			PushTask(a->tasks, HELP);
 			return;
 		}
-		else if(!lstrcmpi("/B", argv[i])) a->listopts |= BARE;
-		else if(!lstrcmpi("/F", argv[i])) a->listopts |= FULLCAPT;
-		else if(!lstrcmpi("/P", argv[i])) a->listopts |= SHOWPOS;
-		else if(!lstrcmpi("/T", argv[i])) a->listopts |= SHOWTB;
-		else if(!lstrcmpi("/CW", argv[i])) PushTask(a->tasks, CW);
-		else if(!lstrcmpi("/TV", argv[i])) PushTask(a->tasks, TV);
-		else if(!lstrcmpi("/TH", argv[i])) PushTask(a->tasks, TH);
-		else if(!lstrcmpi("/MA", argv[i])) PushTask(a->tasks, MA);
-		else if(!lstrcmpi("/UM", argv[i])) PushTask(a->tasks, UW); // maintain /um for compatability with old scripts
-		else if(!lstrcmpi("/UW", argv[i])) PushTask(a->tasks, UW);
-		else if(!lstrcmpi("/AT", argv[i])) PushTask(a->tasks, AT);
-		else if(!lstrcmpi("/FS", argv[i])) PushTask(a->tasks, FS);
-		else if(!lstrcmpi("/WM", argv[i])) PushTask(a->tasks, WM);
-		else if(!lstrcmpi("/DBM", argv[i])) a->actopts |= DONTBLAMEME;
-		else if(!lstrcmpi("/MIN", argv[i])) PushTask(a->tasks, MIN);
-		else if(!lstrcmpi("/MAX", argv[i])) PushTask(a->tasks, MAX);
-		else if(!lstrcmpi("/RES", argv[i])) PushTask(a->tasks, RES);
-		else if(!lstrcmpi("/ACT", argv[i])) PushTask(a->tasks, ACT);
-		else if(!lstrcmpi("/INA", argv[i])) PushTask(a->tasks, INA);
-		else if(!lstrcmpi("/ENA", argv[i])) PushTask(a->tasks, ENA);
-		else if(!lstrcmpi("/DIS", argv[i])) PushTask(a->tasks, DIS);
-		else if(!lstrcmpi("/HID", argv[i])) PushTask(a->tasks, HID);
-		else if(!lstrcmpi("/VIS", argv[i])) PushTask(a->tasks, VIS);
-		else if(!lstrcmpi("/END", argv[i])) PushTask(a->tasks, END);
-		else if(!lstrcmpi("/CLS", argv[i])) PushTask(a->tasks, CLS);
-		else if(!lstrcmpi("/TOP", argv[i])) PushTask(a->tasks, TOP);
-		else if(!lstrcmpi("/NOT", argv[i])) PushTask(a->tasks, NOT);
-		else if(!lstrcmpi("/MOV", argv[i])) {
+		else if(!lstrcmpi("B", argv[i] + 1)) a->listopts |= BARE;
+		else if(!lstrcmpi("F", argv[i] + 1)) a->listopts |= FULLCAPT;
+		else if(!lstrcmpi("P", argv[i] + 1)) a->listopts |= SHOWPOS;
+		else if(!lstrcmpi("T", argv[i] + 1)) a->listopts |= SHOWTB;
+		else if(!lstrcmpi("CW", argv[i] + 1)) PushTask(a->tasks, CW);
+		else if(!lstrcmpi("TV", argv[i] + 1)) PushTask(a->tasks, TV);
+		else if(!lstrcmpi("TH", argv[i] + 1)) PushTask(a->tasks, TH);
+		else if(!lstrcmpi("MA", argv[i] + 1)) PushTask(a->tasks, MA);
+		else if(!lstrcmpi("UM", argv[i] + 1)) PushTask(a->tasks, UW); // maintain /um for compatability with old scripts
+		else if(!lstrcmpi("UW", argv[i] + 1)) PushTask(a->tasks, UW);
+		else if(!lstrcmpi("AT", argv[i] + 1)) PushTask(a->tasks, AT);
+		else if(!lstrcmpi("FS", argv[i] + 1)) PushTask(a->tasks, FS);
+		else if(!lstrcmpi("WM", argv[i] + 1)) PushTask(a->tasks, WM);
+		else if(!lstrcmpi("DBM", argv[i] + 1)) a->actopts |= DONTBLAMEME;
+		else if(!lstrcmpi("MIN", argv[i] + 1)) PushTask(a->tasks, MIN);
+		else if(!lstrcmpi("MAX", argv[i] + 1)) PushTask(a->tasks, MAX);
+		else if(!lstrcmpi("RES", argv[i] + 1)) PushTask(a->tasks, RES);
+		else if(!lstrcmpi("ACT", argv[i] + 1)) PushTask(a->tasks, ACT);
+		else if(!lstrcmpi("INA", argv[i] + 1)) PushTask(a->tasks, INA);
+		else if(!lstrcmpi("ENA", argv[i] + 1)) PushTask(a->tasks, ENA);
+		else if(!lstrcmpi("DIS", argv[i] + 1)) PushTask(a->tasks, DIS);
+		else if(!lstrcmpi("HID", argv[i] + 1)) PushTask(a->tasks, HID);
+		else if(!lstrcmpi("VIS", argv[i] + 1)) PushTask(a->tasks, VIS);
+		else if(!lstrcmpi("END", argv[i] + 1)) PushTask(a->tasks, END);
+		else if(!lstrcmpi("CLS", argv[i] + 1)) PushTask(a->tasks, CLS);
+		else if(!lstrcmpi("TOP", argv[i] + 1)) PushTask(a->tasks, TOP);
+		else if(!lstrcmpi("NOT", argv[i] + 1)) PushTask(a->tasks, NOT);
+		else if(!lstrcmpi("MOV", argv[i] + 1)) {
 			PushTask(a->tasks, MOV);
 			//
 			// next two args MUST be the new left and top coords
@@ -87,7 +90,7 @@ void ParseArgs(int argc, char *argv[], s
 				}
 			else Quit(MOVERR);
 			}
-		else if(!lstrcmpi("/SIZ", argv[i])) {
+		else if(!lstrcmpi("SIZ", argv[i] + 1)) {
 			//
 			// next two args MUST be the new width and height
 			//
@@ -100,7 +103,7 @@ void ParseArgs(int argc, char *argv[], s
 				}
 			else Quit(SIZERR);
 			}
-		else if(!lstrcmpi("/REN", argv[i])) {
+		else if(!lstrcmpi("REN", argv[i] + 1)) {
 			//
 			// next arg MUST be the new caption
 			//
@@ -108,7 +111,7 @@ void ParseArgs(int argc, char *argv[], s
 			if( (i+1) < argc) LoadString(&a->newcapt, argv[++i]);
 			else Quit(RENERR);
 			}
-		else if(!lstrcmpi("/_RENAME_MYCONSOLE_WINDOW", argv[i])) {
+		else if(!lstrcmpi("_RENAME_MYCONSOLE_WINDOW", argv[i] + 1)) {
 			//
 			// next arg MUST be the new caption
 			//
@@ -116,16 +119,19 @@ void ParseArgs(int argc, char *argv[], s
 			if( (i+1) < argc) LoadString(&a->newcapt, argv[++i]);
 			else Quit(RENERR);
 			}
-		else if(!lstrcmpi("/RUN", argv[i])) {
+		else if(!lstrcmpi("RUN", argv[i] + 1)) {
 			PushTask(a->tasks, RUN);
 			//
 			// point to next arg and see if show state has been specced
 			//
-			if( !( ( (i++) +1 ) < argc)) Quit(INCARG);
-			if(!lstrcmpi("/MIN", argv[i])) a->sw_state = SW_SHOWMINIMIZED;
-			else if(!lstrcmpi("/MAX", argv[i])) a->sw_state = SW_SHOWMAXIMIZED;
-			else if(!lstrcmpi("/HID", argv[i])) a->sw_state = SW_HIDE;
-			else --i;
+			if(! ((i+1) < argc)) Quit(INCARG);
+                        if ((*argv[i] == '-') || (*argv[i] == '/')) {
+                            i++;
+                            if(!lstrcmpi("MIN", argv[i] + 1)) a->sw_state = SW_SHOWMINIMIZED;
+                            else if(!lstrcmpi("MAX", argv[i] + 1)) a->sw_state = SW_SHOWMAXIMIZED;
+                            else if(!lstrcmpi("HID", argv[i] + 1)) a->sw_state = SW_HIDE;
+                            else i--;
+                        }
 			//
 			// next arg MUST be file to open/run
 			//
@@ -139,6 +145,8 @@ void ParseArgs(int argc, char *argv[], s
 				i = argc; // prevent any more iterations of for loop
 			}
 		}
+                else Quit(UNRARG); // caption must be first arg
+            }
 		else {
 			//
 			// got this far, so assume unrecognized arg is a caption
@@ -253,7 +261,7 @@ HWND atoHandle(const char *src)
 
 	// string must begin with 0x or 0X
 	if( strncmp("0x", src, 2) && strncmp("0X", src, 2) ) return (HWND) (0);
-	h = (HWND) strtoul(src, &p, 16);
+	h = (HWND) strtoull(src, &p, 16);
 	// all characters must be interpreted
 	if(*p) return (HWND) (0);
 	else return h;
diff -puw ritchielawrence-cmdow-1bbcd2b/header.h cmdow-1.4.8-43/header.h
--- ritchielawrence-cmdow-1bbcd2b/header.h	2016-10-23 22:54:49.000000000 +0200
+++ cmdow-1.4.8-43/header.h	2022-03-02 10:26:54.578143000 +0100
@@ -1,4 +1,10 @@
-//#ifdef _MERGE_RDATA_
+#ifndef _HEADER_H
+#define _HEADER_H
+
+#if __cplusplus >= 201703L
+#define register
+#endif
+
 #define WIN32_LEAN_AND_MEAN		// Exclude rarely-used stuff from Windows headers
 #define _WIN32_WINNT 0x0500
 
@@ -80,6 +86,7 @@ struct ARGS {
 	char			*params;		/* Commandline params used with /RUN */
 	int				sw_state;		/* How window is displayed, used with /RUN */
 	char			*helpcmd;		/* Holds command that user wants help with */
+	int			switchar;		/* Holds character that user wants to mark options */
 };
 
 struct WLIST {
@@ -109,7 +116,7 @@ char *GetArgs();
 char *GetRestCmdline(char *Cmd);
 
 /* help.c functions */
-void ShowHelp(char *);
+void ShowHelp(int, const char *);
 void Quit(const int Err);
 
 /* tlist.c functions */
@@ -149,3 +156,5 @@ void TopWin(struct WLIST *w, struct ARGS
 void NotWin(struct WLIST *w, struct ARGS *a);
 
 int __cdecl _ConvertCommandLineToArgcArgv( void );
+
+#endif
diff -puw ritchielawrence-cmdow-1bbcd2b/help.cpp cmdow-1.4.8-43/help.cpp
--- ritchielawrence-cmdow-1bbcd2b/help.cpp	2016-10-23 22:54:49.000000000 +0200
+++ cmdow-1.4.8-43/help.cpp	2022-03-02 11:37:15.924976000 +0100
@@ -1,33 +1,36 @@
 #include "header.h"
 
-void ShowHelp(char *cmd)
+void ShowHelp(int switchar, const char *cmd)
 {
+	if (cmd == NULL) cmd = "";
+	if ((*cmd == '-') || (*cmd == '/')) ++cmd;
 	if( (!lstrcmpi("window", cmd)) ||
-		(!lstrcmpi("/window", cmd)) ||
-        (!lstrcmpi("/T", cmd)) ||
-		(!lstrcmpi("/B", cmd)) ||
-		(!lstrcmpi("/F", cmd)) ||
-		(!lstrcmpi("/P", cmd))) {
+		(!lstrcmpi("T", cmd)) ||
+		(!lstrcmpi("B", cmd)) ||
+		(!lstrcmpi("F", cmd)) ||
+		(!lstrcmpi("P", cmd))) {
 		printf(
 		"This form of CMDOW lists window details.\n\n"
 //		 ----+----1----+----2----+----3----+----4----+----5----+----6----+----7----+----8
-		"  CMDOW [window | /T] [/B] [/F] [/P]\n\n"
+		"  CMDOW [window | %cT] [%cB] [%cF] [%cP]\n\n"
 //		 ----+----1----+----2----+----3----+----4----+----5----+----6----+----7----+----8
 		"  window  Window to list. Specify a window by its handle (in hex format, eg\n"
 		"          0x1A0142) or caption (window title). Caption is case insensitive.\n"
         "          Use asterisk before/after caption to match zero or more characters.\n"
 		"          Windows without captions are matched by their ClassName. Use double\n"
         "          quotes for captions containing whitespace or control characters. The\n"
-		"          At symbol '@' may be used to refer to this window. eg 'CMDOW @ /HID'.\n"
-		"  /T      List only the windows shown on the taskbar (these are typically\n"
-		"          visible level 1 windows that do not have an owner).\n");
-printf(
-		"  /B      Bare format. Does not print the field names when listing windows.\n"
-		"  /F      Full format. By default CMDOW truncates Image names to eight\n"
+		"          At symbol '@' may be used to refer to this window. eg 'CMDOW @ %cHID'.\n"
+		"  %cT      List only the windows shown on the taskbar (these are typically\n"
+		"          visible level 1 windows that do not have an owner).\n"
+		"  %cB      Bare format. Does not print the field names when listing windows.\n"
+		"  %cF      Full format. By default CMDOW truncates Image names to eight\n"
 		"          characters and Captions/ClassNames to fit under 80 characters.\n"
 		"          Use this option to disable this feature.\n"
-		"  /P      Shows window position, coords of top left corner of the window and\n"
-		"          its absolute width and height in pixels.\n\n");
+		"  %cP      Shows window position, coords of top left corner of the window and\n"
+		"          its absolute width and height in pixels.\n\n",
+		switchar, switchar, switchar,
+		switchar, switchar, switchar,
+		switchar, switchar, switchar);
 		printf(
 		"  Here is a description of the output fields:-\n\n"
 //		 ----+----1----+----2----+----3----+----4----+----5----+----6----+----7----+----8
@@ -38,8 +41,7 @@ printf(
 		"                 level 2+ are child windows.\n"
 		"  Pid            Process Id of the window (Task Manager also shows Pids).\n"
 		"  Window status  How the window is displayed (Minimized, Maximized, Restored,\n"
-		"                 Active, Inactive, Enabled, Disabled, Visible or Hidden).\n");
-		printf(
+		"                 Active, Inactive, Enabled, Disabled, Visible or Hidden).\n"
 		"  Left, Top      Coords of left top corner of the window relative to the\n"
 		"                 parent window. For top level windows this is the desktop, and\n"
 		"                 for child windows it is their top level window.\n"
@@ -50,67 +52,68 @@ printf(
 		"                 listed instead.\n\n");
 //		 ----+----1----+----2----+----3----+----4----+----5----+----6----+----7----+----8
 	}
-	else if((!lstrcmpi("/TH", cmd)) ||
-			(!lstrcmpi("/TV", cmd)) ||
-			(!lstrcmpi("/MA", cmd)) ||
-			(!lstrcmpi("/UM", cmd)) ||
-			(!lstrcmpi("/UW", cmd)) ||
-			(!lstrcmpi("/CW", cmd)) ||
-			(!lstrcmpi("/AT", cmd)) ||
-			(!lstrcmpi("/FS", cmd)) ||
-			(!lstrcmpi("/WM", cmd))) {
+	else if((!lstrcmpi("TH", cmd)) ||
+			(!lstrcmpi("TV", cmd)) ||
+			(!lstrcmpi("MA", cmd)) ||
+			(!lstrcmpi("UM", cmd)) ||
+			(!lstrcmpi("UW", cmd)) ||
+			(!lstrcmpi("CW", cmd)) ||
+			(!lstrcmpi("AT", cmd)) ||
+			(!lstrcmpi("FS", cmd)) ||
+			(!lstrcmpi("WM", cmd))) {
 		printf(
 		"This form of CMDOW performs a single action typically affecting all windows.\n\n"
 //		 ----+----1----+----2----+----3----+----4----+----5----+----6----+----7----+----8
-		"  CMDOW /TH | /TV | /CW | /MA | /UW | /AT | /FS | /WM\n\n"
+		"  CMDOW %cTH | %cTV | %cCW | %cMA | %cUW | %cAT | %cFS | %cWM\n\n"
 //		 ----+----1----+----2----+----3----+----4----+----5----+----6----+----7----+----8
-		"  /TH     Tiles all windows horizontally. Functionally equivalent to right-\n"
+		"  %cTH     Tiles all windows horizontally. Functionally equivalent to right-\n"
 		"          clicking the taskbar and selecting 'Tile Windows Horizontally'.\n"
-		"  /TV     Tiles all windows vertically. Functionally equivalent to right-\n"
+		"  %cTV     Tiles all windows vertically. Functionally equivalent to right-\n"
 		"          clicking the taskbar and selecting 'Tile Windows Vertically'.\n"
-		"  /CW     Functionally equivalent to right-clicking the taskbar and selecting\n"
-		"          'Cascade Windows'.\n");
-		printf(
-		"  /MA     Minimizes all windows. Functionally equivalent to right-clicking the\n"
+		"  %cCW     Functionally equivalent to right-clicking the taskbar and selecting\n"
+		"          'Cascade Windows'.\n"
+		"  %cMA     Minimizes all windows. Functionally equivalent to right-clicking the\n"
 		"          taskbar and selecting 'Minimize All Windows' or using the keyboard\n"
 		"          shortcut Win+M. A window may not be minimized if it has a modal\n"
 		"          dialog box open (typically the window displayed by Help | About).\n"
-		"  /UW     Undo the effect of /TH, /TV, /CW and /MA. Functionally equivalent to\n"
+		"  %cUW     Undo the effect of /TH, /TV, /CW and /MA. Functionally equivalent to\n"
 		"          right-clicking the taskbar and selecting 'Undo...' or using the\n"
-		"          keyboard shortcut Win+Shift+M.\n");
-		printf(
-		"  /AT     Similar to the keyboard shortcut Alt+Tab but instead of activating\n"
+		"          keyboard shortcut Win+Shift+M.\n"
+		"  %cAT     Similar to the keyboard shortcut Alt+Tab but instead of activating\n"
 		"          the next highest window in the Z order, /AT activates the window at\n"
 		"          the bottom of the Z order. Consequently, /AT can be used to activate\n"
 		"          every window on the taskbar sequentially. To prevent windows on the \n"
 		"          taskbar from flashing, set the following registry value to zero:-\n"
-		"          HKCU\\Control Panel\\Desktop\\ForegroundLockTimeout.\n");
-		printf(
-		"  /FS     Switch the window running this process into full screen mode.\n"
-		"  /WM     Switch the window running this process into window mode.\n\n");
+		"          HKCU\\Control Panel\\Desktop\\ForegroundLockTimeout.\n"
+		"  %cFS     Switch the window running this process into full screen mode.\n"
+		"  %cWM     Switch the window running this process into window mode.\n\n",
+		switchar, switchar, switchar, switchar,
+		switchar, switchar, switchar, switchar,
+		switchar, switchar, switchar, switchar,
+		switchar, switchar, switchar, switchar);
 //		 ----+----1----+----2----+----3----+----4----+----5----+----6----+----7----+----8
 		printf(
 		"  This example batch file activates a different window every ten seconds:-\n\n"
 
 		"    @ECHO OFF\n"
 		"    :LOOP\n"
-		"    CMDOW /AT&PING 127.0.0.1 -n 11 >NUL&GOTO :LOOP\n");
+		"    CMDOW %cAT&PING 127.0.0.1 -n 11 >NUL&GOTO :LOOP\n", switchar);
 	}
-	else if((!lstrcmpi("/ACT", cmd)) ||
-			(!lstrcmpi("/INA", cmd)) ||
-			(!lstrcmpi("/ENA", cmd)) ||
-			(!lstrcmpi("/DIS", cmd)) ||
-			(!lstrcmpi("/VIS", cmd)) ||
-			(!lstrcmpi("/HID", cmd)) ||
-			(!lstrcmpi("/MIN", cmd)) ||
-			(!lstrcmpi("/MAX", cmd)) ||
-			(!lstrcmpi("/RES", cmd))) {
+	else if((!lstrcmpi("ACT", cmd)) ||
+			(!lstrcmpi("INA", cmd)) ||
+			(!lstrcmpi("ENA", cmd)) ||
+			(!lstrcmpi("DIS", cmd)) ||
+			(!lstrcmpi("VIS", cmd)) ||
+			(!lstrcmpi("HID", cmd)) ||
+			(!lstrcmpi("MIN", cmd)) ||
+			(!lstrcmpi("MAX", cmd)) ||
+			(!lstrcmpi("RES", cmd))) {
 		printf(
 		"This form of CMDOW performs specified action(s) on the specified window(s).\n"
 		"Some of these commands allow you to manipulate windows in ways not normally\n"
 		"possible. Improper use may cause unexpected results and system instability.\n\n"
 //		 ----+----1----+----2----+----3----+----4----+----5----+----6----+----7----+----8
-		"  CMDOW window {[/ACT] [/INA] [/ENA] [/DIS] [/VIS] [/HID] [/MIN] [/MAX] [/RES]}\n\n"
+		"  CMDOW window {[%cACT] [%cINA] [%cENA] [%cDIS] [%cVIS] [%cHID] [%cMIN] [%cMAX] [%cRES]}\n\n"
 //		 ----+----1----+----2----+----3----+----4----+----5----+----6----+----7----+----8
 		"  window  Window to act on. Specify a window by its handle (in hex format, eg\n"
 		"          0x1A0142) or caption (window title). Caption is case insensitive.\n"
@@ -118,60 +121,61 @@ printf(
         "          Windows without captions are matched by their ClassName. Use double\n"
         "          quotes for captions containing whitespace or control characters. The\n"
 		"          At symbol '@' may be used to refer to this window. eg 'CMDOW @ /HID'.\n"
-		"  /ACT    Activates the specified window. It's possible to activate a hidden\n"
+		"  %cACT    Activates the specified window. It's possible to activate a hidden\n"
 		"          and/or disabled window (although doing so will not make it visible or\n"
-		"          enabled). If a minimized window is activated, it remains minimized.\n");
-		printf(
+		"          enabled). If a minimized window is activated, it remains minimized.\n"
 //		 ----+----1----+----2----+----3----+----4----+----5----+----6----+----7----+----8
-		"  /INA    Inactivates the specified window (activates a window shown on the\n"
+		"  %cINA    Inactivates the specified window (activates a window shown on the\n"
 		"          taskbar with the lowest Z order). If the specified window is already\n"
 		"          inactive or it is the only window shown on the taskbar, then /INA has\n"
 		"          no effect. This action added for completeness rather than usefulness.\n"
-		"  /ENA    Enables the specified window (allowing user interaction).\n"
-		"  /DIS    Disables the specified window. A disabled window will continue to run\n"
+		"  %cENA    Enables the specified window (allowing user interaction).\n"
+		"  %cDIS    Disables the specified window. A disabled window will continue to run\n"
 		"          as usual, although it will not accept user input until it is enabled.\n"
-		"  /VIS    Unhides the specified window (make it visible).\n"
-		"  /HID    Hides the specified window. Although a hidden window will not be\n"
-		"          shown on the taskbar, it will still run and may be unhidden later.\n");
-		printf(
-		"  /MIN    Minimizes specified window. Not all windows can be minimized, dialog\n"
+		"  %cVIS    Unhides the specified window (make it visible).\n"
+		"  %cHID    Hides the specified window. Although a hidden window will not be\n"
+		"          shown on the taskbar, it will still run and may be unhidden later.\n"
+		"  %cMIN    Minimizes specified window. Not all windows can be minimized, dialog\n"
 		"          boxes for instance.\n"
-		"  /MAX    Maximizes specified window (even those not having a Maximize button).\n"
-		"  /RES    Restores the specified window to its former size and position. If a\n"
+		"  %cMAX    Maximizes specified window (even those not having a Maximize button).\n"
+		"  %cRES    Restores the specified window to its former size and position. If a\n"
 		"          minimized window was originally maximized, then /RES will maximize\n"
 		"          the window. Applying /RES a second time, will restore the window to\n"
-		"          the size and position it was before it was maximized.\n\n"
+		"          the size and position it was before it was maximized.\n\n",
+		switchar, switchar, switchar,
+		switchar, switchar, switchar,
+		switchar, switchar, switchar,
+		switchar, switchar, switchar,
+		switchar, switchar, switchar,
+		switchar, switchar, switchar);
+		printf(
 //		 ----+----1----+----2----+----3----+----4----+----5----+----6----+----7----+----8
 		"  If more than one window matches the specified caption, CMDOW only acts on the\n"
-		"  first one. To override this add the /DBM (Don't Blame Me) switch - DANGEROUS.\n"
+		"  first one. To override this add the %cDBM (Don't Blame Me) switch - DANGEROUS.\n"
 		"  Multiple actions are performed in order, from left to right. Eg:-\n\n"
 
-		"    CMDOW 0x0E0144 /hid /ren \"10%% complete\" /mov 0 0 /siz 300 100 /act /vis\n");
+		"    CMDOW 0x0E0144 %chid %cren \"10%% complete\" %cmov 0 0 %csiz 300 100 %cact %cvis\n",
+		switchar, switchar, switchar,
+		switchar, switchar, switchar, switchar);
 	}
 	else if((!lstrcmpi("caption", cmd)) ||
-			(!lstrcmpi("/caption", cmd)) ||
-			(!lstrcmpi("/REN", cmd)) ||
-			(!lstrcmpi("/MOV", cmd)) ||
-			(!lstrcmpi("top", cmd)) ||
-			(!lstrcmpi("/top", cmd)) ||
+			(!lstrcmpi("REN", cmd)) ||
+			(!lstrcmpi("MOV", cmd)) ||
 			(!lstrcmpi("left", cmd)) ||
-			(!lstrcmpi("/left", cmd)) ||
-			(!lstrcmpi("/SIZ", cmd)) ||
+			(!lstrcmpi("SIZ", cmd)) ||
 			(!lstrcmpi("width", cmd)) ||
-			(!lstrcmpi("/width", cmd)) ||
 			(!lstrcmpi("height", cmd)) ||
-			(!lstrcmpi("/height", cmd)) ||
-			(!lstrcmpi("/TOP", cmd)) ||
-			(!lstrcmpi("/NOT", cmd)) ||
-			(!lstrcmpi("/CLS", cmd)) ||
-			(!lstrcmpi("/END", cmd))) {
+			(!lstrcmpi("TOP", cmd)) ||
+			(!lstrcmpi("NOT", cmd)) ||
+			(!lstrcmpi("CLS", cmd)) ||
+			(!lstrcmpi("END", cmd))) {
 		printf(
 		"This form of CMDOW performs specified action(s) on the specified window(s).\n"
 		"Some of these commands allow you to manipulate windows in ways not normally\n"
 		"possible. Improper use may cause unexpected results and system instability.\n\n"
 //		 ----+----1----+----2----+----3----+----4----+----5----+----6----+----7----+----8
-		"  CMDOW window {[/REN newcaption] [/TOP] [/NOT] [/MOV left top]\n"
-		"        [/SIZ width height] [/CLS] [/END]}\n\n"
+		"  CMDOW window {[%cREN newcaption] [%cTOP] [%cNOT] [%cMOV left top]\n"
+		"        [%cSIZ width height] [%cCLS] [%cEND]}\n\n"
 //		 ----+----1----+----2----+----3----+----4----+----5----+----6----+----7----+----8
 		"  window  Window to act on. Specify a window by its handle (in hex format, eg\n"
 		"          0x1A0142) or caption (window title). Caption is case insensitive.\n"
@@ -179,124 +183,137 @@ printf(
         "          Windows without captions are matched by their ClassName. Use double\n"
         "          quotes for captions containing whitespace or control characters. The\n"
 		"          At symbol '@' may be used to refer to this window. eg 'CMDOW @ /HID'.\n"
-		"  /REN    Renames the caption of the specified window to the caption specified\n"
-		"          after /REN. Use double quotes for captions containing whitespace.\n");
-		printf(
-		"  /TOP    Makes the specified window always on top. It will remain on top even\n"
+		"  %cREN    Renames the caption of the specified window to the caption specified\n"
+		"          after /REN. Use double quotes for captions containing whitespace.\n"
+		"  %cTOP    Makes the specified window always on top. It will remain on top even\n"
 		"          when another window is activated. If two or more windows have this\n"
 		"          attribute set, the topmost window will be that which had the\n"
 		"          attribute set first.\n"
-		"  /NOT    Makes window not always on top. This is the normal setting for most\n"
-		"          windows.\n");
-		printf(
-		"  /MOV    Moves the specified window. The left and top arguments specify the\n"
+		"  %cNOT    Makes window not always on top. This is the normal setting for most\n"
+		"          windows.\n"
+		"  %cMOV    Moves the specified window. The left and top arguments specify the\n"
 		"          new position of the left top corner of the window. For level one\n"
 		"          windows this is relative to the desktop, and for child windows this\n"
 		"          is relative to its parent window. Level one windows can be moved off\n"
 		"          the screen using negative coords or coords greater than the desktop\n"
 		"          size. You should not move a minimized or maximized window without\n"
 		"          first restoring it.\n"
-		"  /SIZ    Resizes the specified window. The width and height arguments specify\n"
+		"  %cSIZ    Resizes the specified window. The width and height arguments specify\n"
 		"          the new absolute width and height. In general, windows cannot be made\n"
 		"          smaller using /SIZ than they could if using conventional methods. You\n"
 		"          should not resize a minimized or maximized window without first\n"
-		"          restoring it.\n");
-		printf(
-		"  /CLS    Politely asks the specified window to close. Most applications will\n"
+		"          restoring it.\n"
+		"  %cCLS    Politely asks the specified window to close. Most applications will\n"
 		"          respond to this request, although some may display a message box\n"
 		"          asking you what to do with unsaved work.\n"
-		"  /END    Kills the process associated with the specified window, the result is\n"
-		"          all windows with the same Pid are also killed. Use very carefully.\n\n");
+		"  %cEND    Kills the process associated with the specified window, the result is\n"
+		"          all windows with the same Pid are also killed. Use very carefully.\n\n",
+		switchar, switchar, switchar, switchar,
+		switchar, switchar, switchar, switchar,
+		switchar, switchar, switchar, switchar,
+		switchar, switchar);
 //		 ----+----1----+----2----+----3----+----4----+----5----+----6----+----7----+----8
 		printf(
 		"  If more than one window matches the specified caption, CMDOW only acts on the\n"
-		"  first one. To override this add the /DBM (Don't Blame Me) switch - DANGEROUS.\n"
+		"  first one. To override this add the %cDBM (Don't Blame Me) switch - DANGEROUS.\n"
 		"  Multiple actions are performed in order, from left to right. Eg:-\n\n"
 
-		"    CMDOW 0x0E0144 /hid /ren \"10%% complete\" /mov 0 0 /siz 300 100 /act /vis\n");
+		"    CMDOW 0x0E0144 %chid %cren \"10%% complete\" %cmov 0 0 %csiz 300 100 %cact %cvis\n",
+		switchar, switchar, switchar, switchar,
+		switchar, switchar, switchar);
 	}
-	else if((!lstrcmpi("/RUN", cmd)) ||
+	else if((!lstrcmpi("RUN", cmd)) ||
 			(!lstrcmpi("state", cmd)) ||
-			(!lstrcmpi("/state", cmd)) ||
 			(!lstrcmpi("file", cmd)) ||
-			(!lstrcmpi("/file", cmd)) ||
-			(!lstrcmpi("args", cmd)) ||
-			(!lstrcmpi("/args", cmd))) {
+			(!lstrcmpi("args", cmd))) {
 		printf(
 		"This form of CMDOW executes the specified file or opens it using the\n"
 		"application associated with the filename extension.\n\n"
 //		 ----+----1----+----2----+----3----+----4----+----5----+----6----+----7----+----8
-		"  CMDOW /RUN [state] file [args]\n\n"
+		"  CMDOW %cRUN [state] file [args]\n\n"
 //		 ----+----1----+----2----+----3----+----4----+----5----+----6----+----7----+----8
-		"  /RUN    Executes the specified file if possible, otherwise opens it using the\n"
+		"  %cRUN    Executes the specified file if possible, otherwise opens it using the\n"
 		"          application associated with the filename extension.\n"
 		"  state   Requests how the application's window is initially displayed. Use\n"
-		"          /MIN for minimized, /MAX for maximized and /HID for hidden. If\n"
+		"          %cMIN for minimized, %cMAX for maximized and %cHID for hidden. If\n"
 		"          omitted the application is started in its default show state. Some\n"
 		"          applications ignore this, use CMDOW to alter the window state after\n"
-		"          the application has started.\n");
-		printf(
+		"          the application has started.\n"
 		"  file    File to execute/open. Use double quotes if filename contains spaces.\n"
 		"  args    Optional arguments passed on to the application. Specify any args\n"
-		"          exactly as you would if executing/opening the file directly.\n\n"
+		"          exactly as you would if executing/opening the file directly.\n\n",
+		switchar, switchar, switchar, switchar, switchar);
+		printf(
 //		 ----+----1----+----2----+----3----+----4----+----5----+----6----+----7----+----8
 		"  Eg /P instructs Notepad to send the specified file to the default printer,\n"
 		"  then quit. This example prints readme.txt without displaying any windows:-\n\n"
 
-		"    CMDOW /RUN /HID notepad /P readme.txt\n\n");
+		"    CMDOW %cRUN %cHID notepad /P readme.txt\n\n",
+		switchar, switchar);
 //		 ----+----1----+----2----+----3----+----4----+----5----+----6----+----7----+----8
 		printf(
 		"  CMDOW can be used to create autorun CDs that work on all Win32 platforms.\n"
 		"  Copy CMDOW.EXE to the CD and create an autorun.inf file. Here is a sample:-\n\n"
 //		 ----+----1----+----2----+----3----+----4----+----5----+----6----+----7----+----8
 		"    [autorun]\n"
-		"    open=cmdow /run /max \\video.mpg\n"
+		"    open=cmdow %crun %cmax \\video.mpg\n"
 		"    icon=myicon.ico\n"
 		"    shell\\readme=Read &Me\n"
-		"    shell\\readme\\command=cmdow /run \\readme.htm\n");
+		"    shell\\readme\\command=cmdow %crun \\readme.htm\n",
+		switchar, switchar, switchar);
 	}
 	else {
 		printf(
-		"CMDOW [Version 1.4.8] Win32 Commandline Window Utility for NT4/2000/XP/7.\n"
+		"CMDOW [Version 1.4.8] Win32 Commandline Window Utility for NT4/2000/XP/7/8/10/11.\n"
 		"(C) Copyright 2001-2014 Ritchie Lawrence, http://www.commandline.co.uk.\n\n"
 //		 ----+----1----+----2----+----3----+----4----+----5----+----6----+----7----+----8
-		"  CMDOW [window | /T] [/B] [/F] [/P]\n"
-		"  CMDOW /TH | /TV | /MA | /CW | /UW | /AT | /FS | /WM\n"
-		"  CMDOW window {[/ACT] [/INA] [/ENA] [/DIS] [/VIS] [/HID] [/MIN] [/MAX] [/RES]\n"
-		"        [/REN caption] [/TOP] [/NOT] [/MOV left top] [/SIZ width height] [/CLS]\n"
-		"        [/END]}\n"
-		"  CMDOW /RUN [state] file [args]\n\n");
+		"  CMDOW [window | %cT] [%cB] [%cF] [%cP]\n"
+		"  CMDOW %cTH | %cTV | %cMA | %cCW | %cUW | %cAT | %cFS | %cWM\n"
+		"  CMDOW window {[%cACT] [%cINA] [%cENA] [%cDIS] [%cVIS] [%cHID] [%cMIN] [%cMAX] [%cRES]\n"
+		"        [%cREN caption] [%cTOP] [%cNOT] [%cMOV left top] [%cSIZ width height] [%cCLS]\n"
+		"        [%cEND]}\n"
+		"  CMDOW %cRUN [state] file [args]\n\n",
+		switchar, switchar, switchar, switchar, switchar,
+		switchar, switchar, switchar, switchar, switchar,
+		switchar, switchar, switchar, switchar, switchar,
+		switchar, switchar, switchar, switchar, switchar,
+		switchar, switchar, switchar, switchar, switchar,
+		switchar, switchar, switchar, switchar);
 //		 ----+----1----+----2----+----3----+----4----+----5----+----6----+----7----+----8
 		printf(
 		"  window  List specified window(s) (if omitted, all windows are listed).\n"
-		"  /T      List windows only shown on the taskbar.\n"
-		"  /B      List windows using bare format (no heading information).\n"
-		"  /F      List windows showing full information (don't truncate any fields).\n"
-		"  /P      List windows showing position and size (left, top, width and height).\n\n"
-//		 ----+----1----+----2----+----3----+----4----+----5----+----6----+----7----+----8
-		"  /TH     Tile windows horizontally.    /TV     Tile windows vertically.\n"
-		"  /MA     Minimize all windows.         /CW     Cascade windows.\n"
-		"  /UW     Undo tile/minimize/cascade.   /AT     Intelligent [Alt]-[Tab].\n"
-		"  /FS     Switch to full screen mode.   /WM     Switch to window mode.\n\n");
-//		 ----+----1----+----2----+----3----+----4----+----5----+----6----+----7----+----8
-		printf(
-		"  /ACT    Activate specified window.    /INA    Inactivate specified window.\n"
-		"  /ENA    Enable specified window.      /DIS    Disable specified window.\n"
-		"  /VIS    Unhide specified window.      /HID    Hide specified window.\n"
-		"  /MIN    Minimize specified window.    /MAX    Maximize specified window.\n"
-		"  /RES    Restore specified window.     /REN    Rename specified window.\n"
-		"  /TOP    Make window always on top.    /NOT    Make window not always on top.\n"
-		"  /MOV    Move specified window.        /SIZ    Resize specified window.\n"
-		"  /CLS    Close specified window.       /END    Kill process linked to window.\n\n");
-//		 ----+----1----+----2----+----3----+----4----+----5----+----6----+----7----+----8
-		printf(
-		"  /RUN    Executes or opens specified file using associated application.\n"
-		"  state   Initial show state of window (/MIN, /MAX or /HID). Default is normal.\n"
-		"  args    Optional commandline arguments passed to launched application.\n\n");
-		printf(
+		"  %cT      List windows only shown on the taskbar.\n"
+		"  %cB      List windows using bare format (no heading information).\n"
+		"  %cF      List windows showing full information (don't truncate any fields).\n"
+		"  %cP      List windows showing position and size (left, top, width and height).\n\n"
+//		 ----+----1----+----2----+----3----+----4----+----5----+----6----+----7----+----8
+		"  %cTH     Tile windows horizontally.    %cTV     Tile windows vertically.\n"
+		"  %cMA     Minimize all windows.         %cCW     Cascade windows.\n"
+		"  %cUW     Undo tile/minimize/cascade.   %cAT     Intelligent [Alt]-[Tab].\n"
+		"  %cFS     Switch to full screen mode.   %cWM     Switch to window mode.\n\n"
+//		 ----+----1----+----2----+----3----+----4----+----5----+----6----+----7----+----8
+		"  %cACT    Activate specified window.    %cINA    Inactivate specified window.\n"
+		"  %cENA    Enable specified window.      %cDIS    Disable specified window.\n"
+		"  %cVIS    Unhide specified window.      %cHID    Hide specified window.\n"
+		"  %cMIN    Minimize specified window.    %cMAX    Maximize specified window.\n"
+		"  %cRES    Restore specified window.     %cREN    Rename specified window.\n"
+		"  %cTOP    Make window always on top.    %cNOT    Make window not always on top.\n"
+		"  %cMOV    Move specified window.        %cSIZ    Resize specified window.\n"
+		"  %cCLS    Close specified window.       %cEND    Kill process linked to window.\n\n"
+//		 ----+----1----+----2----+----3----+----4----+----5----+----6----+----7----+----8
+		"  %cRUN    Executes or opens specified file using associated application.\n"
+		"  state   Initial show state of window (%cMIN, %cMAX or %cHID). Default is normal.\n"
+		"  args    Optional commandline arguments passed to launched application.\n\n"
 		"  Specify a window by its caption (case insensitive) or handle in hex format.\n"
 		"  Wildcards may be used. Use At symbol to refer to this window. For more help\n"
-		"  use CMDOW /? <parameter>. Eg CMDOW /? /window or CMDOW /? /act.\n");
+		"  use CMDOW %c? <parameter>. Eg CMDOW %c? %cwindow or CMDOW %c? %cact.\n",
+		switchar, switchar, switchar, switchar, switchar,
+		switchar, switchar, switchar, switchar, switchar,
+		switchar, switchar, switchar, switchar, switchar,
+		switchar, switchar, switchar, switchar, switchar,
+		switchar, switchar, switchar, switchar, switchar,
+		switchar, switchar, switchar, switchar, switchar,
+		switchar, switchar, switchar, switchar, switchar,
+		switchar, switchar);
 	}
 }
-
diff -puw ritchielawrence-cmdow-1bbcd2b/main.cpp cmdow-1.4.8-43/main.cpp
--- ritchielawrence-cmdow-1bbcd2b/main.cpp	2016-10-23 22:54:49.000000000 +0200
+++ cmdow-1.4.8-43/main.cpp	2022-03-02 10:24:53.161184500 +0100
@@ -33,7 +33,7 @@ int main(int argc, char* argv[])
 	done = TRUE;
 	switch(*args.tasks) {
 		case HELP:
-			ShowHelp(args.helpcmd);
+                  ShowHelp(args.switchar, args.helpcmd);
 			break;
 		case CW:
 			//CascadeWindows(NULL, 0, NULL, 0, NULL);
@@ -72,6 +72,7 @@ int main(int argc, char* argv[])
 			break;
 		case WM: // switch to window mode
 			res = WINDOW_MODE;
+			// fall through
 		case FS: // switch to full screen mode
 			SCDM = (SetConsoleDisplayMode_)GetProcAddress(GetModuleHandle("kernel32.dll"), "SetConsoleDisplayMode");
 			if(SCDM) SCDM(GetStdHandle(STD_OUTPUT_HANDLE), res, &res);
diff -puw ritchielawrence-cmdow-1bbcd2b/tlist.cpp cmdow-1.4.8-43/tlist.cpp
--- ritchielawrence-cmdow-1bbcd2b/tlist.cpp	2016-10-23 22:54:49.000000000 +0200
+++ cmdow-1.4.8-43/tlist.cpp	2022-03-01 17:25:17.632041800 +0100
@@ -123,8 +123,8 @@ int GetTaskList(struct TLIST *tlist)
 
 	dwProcessIdCounter = 0; /* added by me as var could be used without init */
 
-    pObj = (PPERF_OBJECT_TYPE) ((DWORD)pPerf + pPerf->HeaderLength);
-    pCounterDef = (PPERF_COUNTER_DEFINITION) ((DWORD)pObj + pObj->HeaderLength);
+    pObj = (PPERF_OBJECT_TYPE) ((INT_PTR)pPerf + pPerf->HeaderLength);
+    pCounterDef = (PPERF_COUNTER_DEFINITION) ((INT_PTR)pObj + pObj->HeaderLength);
     for (i=0; i<(DWORD)pObj->NumCounters; i++) {
         if (pCounterDef->CounterNameTitleIndex == dwProcessIdTitle) {
             dwProcessIdCounter = pCounterDef->CounterOffset;
@@ -135,11 +135,11 @@ int GetTaskList(struct TLIST *tlist)
 
     dwNumTasks = (DWORD) pObj->NumInstances;
 
-    pInst = (PPERF_INSTANCE_DEFINITION) ((DWORD)pObj + pObj->DefinitionLength);
+    pInst = (PPERF_INSTANCE_DEFINITION) ((INT_PTR)pObj + pObj->DefinitionLength);
 
     for (i=0; i<dwNumTasks; i++) {
 
-        p = (LPSTR) ((DWORD)pInst + pInst->NameOffset);
+        p = (LPSTR) ((INT_PTR)pInst + pInst->NameOffset);
         rc = WideCharToMultiByte( CP_ACP, 0, (LPCWSTR)p, -1, szProcessName,
 			sizeof(szProcessName), NULL, NULL);
 
@@ -151,9 +151,9 @@ int GetTaskList(struct TLIST *tlist)
 		lstrcpy( tlist->ProcessName, szProcessName );
 
 		// load Pid into tlist
-	    pCounter = (PPERF_COUNTER_BLOCK) ((DWORD)pInst + pInst->ByteLength);
-        tlist->dwProcessId = *((LPDWORD) ((DWORD)pCounter + dwProcessIdCounter));
-        pInst = (PPERF_INSTANCE_DEFINITION) ((DWORD)pCounter + pCounter->ByteLength);
+                pCounter = (PPERF_COUNTER_BLOCK) ((INT_PTR)pInst + pInst->ByteLength);
+            tlist->dwProcessId = *((LPDWORD) ((INT_PTR)pCounter + dwProcessIdCounter));
+            pInst = (PPERF_INSTANCE_DEFINITION) ((INT_PTR)pCounter + pCounter->ByteLength);
 
 		// create a new tlist record
 		tlist->next = (struct TLIST *) HeapAlloc(GetProcessHeap(), 0, sizeof(struct TLIST));
diff -puw ritchielawrence-cmdow-1bbcd2b/wlist.cpp cmdow-1.4.8-43/wlist.cpp
--- ritchielawrence-cmdow-1bbcd2b/wlist.cpp	2016-10-23 22:54:49.000000000 +0200
+++ cmdow-1.4.8-43/wlist.cpp	2022-03-02 11:37:15.506517800 +0100
@@ -259,10 +259,10 @@ void LstWin(struct WLIST *w, struct ARGS
 	VisibleHidden	= (w->styles & WS_VISIBLE)  ? "Vis" : "Hid";
 	EnabledDisabled = (w->styles & WS_DISABLED) ? "Dis" : "Ena";
 
-	printf("0x%.*lX %*i %*lu %s %s %s %s ",
-		   len_handle, (unsigned long) w->hwnd,
+	printf("0x%.*zX %*i %*lu %s %s %s %s ",
+		   len_handle, (size_t)w->hwnd,
 		   len_level, w->level,
-		   len_pid, (unsigned long) w->pid,
+		   len_pid, w->pid,
 	       MinMaxRes, ActiveInactive, EnabledDisabled, VisibleHidden);
 	if(a->listopts & SHOWPOS) {
 		printf("%6d %6d %6d %6d ", w->left, w->top, w->width, w->height);
@@ -344,6 +344,7 @@ void MaxWin(struct WLIST *w, struct ARGS
 {
 	HWND tmp;
 
+	(void)a;
 	//
 	// just in case maximized window gets focus, activate window that had focus
 	//
@@ -358,6 +359,8 @@ void MaxWin(struct WLIST *w, struct ARGS
 void MinWin(struct WLIST *w, struct ARGS *a)
 {
 	HWND tmp;
+
+	(void)a;
 	//
 	// just in case maximized window gets focus, activate window that had focus
 	//
@@ -372,6 +375,8 @@ void MinWin(struct WLIST *w, struct ARGS
 void ResWin(struct WLIST *w, struct ARGS *a)
 {
 	HWND tmp;
+
+	(void)a;
 	tmp = GetForegroundWindow();
 	// if was min, then res may make it active
 	// win was max, then min - then res will max it, res again will restore it
@@ -384,6 +389,7 @@ void ResWin(struct WLIST *w, struct ARGS
 //
 void ActWin(struct WLIST *w, struct ARGS *a)
 {
+	(void)a;
 	SetFGWindow(w->hwnd);
 }
 
@@ -392,6 +398,7 @@ void ActWin(struct WLIST *w, struct ARGS
 //
 void InaWin(struct WLIST *w, struct ARGS *a)
 {
+	(void)a;
 	//
 	// only activate another window if specified window actually has focus
 	//
@@ -404,6 +411,7 @@ void InaWin(struct WLIST *w, struct ARGS
 //
 void EnaWin(struct WLIST *w, struct ARGS *a)
 {
+	(void)a;
 	EnableWindow(w->hwnd, TRUE);
 }
 
@@ -412,6 +420,7 @@ void EnaWin(struct WLIST *w, struct ARGS
 //
 void DisWin(struct WLIST *w, struct ARGS *a)
 {
+	(void)a;
 	EnableWindow(w->hwnd, FALSE);
 }
 
@@ -420,6 +429,7 @@ void DisWin(struct WLIST *w, struct ARGS
 //
 void HidWin(struct WLIST *w, struct ARGS *a)
 {
+	(void)a;
 	ShowWindowAsync(w->hwnd, SW_HIDE);
 }
 
@@ -428,6 +438,7 @@ void HidWin(struct WLIST *w, struct ARGS
 //
 void VisWin(struct WLIST *w, struct ARGS *a)
 {
+	(void)a;
 	ShowWindowAsync(w->hwnd, SW_SHOW);
 }
 
@@ -436,6 +447,7 @@ void VisWin(struct WLIST *w, struct ARGS
 //
 void ClsWin(struct WLIST *w, struct ARGS *a)
 {
+	(void)a;
 	PostMessage(w->hwnd, WM_CLOSE, 0, 0);
 }
 
@@ -446,6 +458,7 @@ void EndWin(struct WLIST *w, struct ARGS
 {
 	HANDLE hProcess;
 
+	(void)a;
 	hProcess = OpenProcess(PROCESS_ALL_ACCESS, FALSE, w->pid);
 	if (hProcess) {
 		hProcess = OpenProcess(PROCESS_ALL_ACCESS, FALSE, w->pid);
@@ -494,7 +507,7 @@ void RenWin(struct WLIST *w, struct ARGS
 	//
 	// Build the commandline to rename this console window
 	//
-	ltoa((long) a->myhwnd, buf, 16);
+	lltoa((INT_PTR)a->myhwnd, buf, 16);
 
 	i = lstrlen(a->exename);
 	i += lstrlen(" 0x");
@@ -562,8 +575,11 @@ void RunWin(struct WLIST *w, struct ARGS
 {
 	#define SHELLEXEC_SUCCESS 33
 	int RetVal;
+
+	(void)w;
+#if 0
 	// FindExecutable() Retrieves the name of and handle to the executable (.exe) file associated with the specified file name
-	/*STARTUPINFO si;
+	STARTUPINFO si;
 	PROCESS_INFORMATION pi;
 	char buf[1024];
 	HMODULE hModule;
@@ -594,10 +610,11 @@ void RunWin(struct WLIST *w, struct ARGS
 		&pi							// processinfo
 	);
 	CloseHandle(pi.hProcess);
-	CloseHandle(pi.hThread);*/
+	CloseHandle(pi.hThread);
 //	printf("file [%s], params [%s]\n", a->file, a->params);
+#endif
 
-	RetVal = (int) ShellExecute(NULL, NULL, a->file, a->params, NULL, a->sw_state);
+	RetVal = (INT_PTR) ShellExecute(NULL, NULL, a->file, a->params, NULL, a->sw_state);
 	if(RetVal < SHELLEXEC_SUCCESS) Quit(EXEERR);
 }
 
@@ -663,6 +680,7 @@ void AltTab(void)
 
 void TopWin(struct WLIST *w, struct ARGS *a)
 {
+	(void)a;
 	SetWindowPos(w->hwnd, HWND_TOPMOST, 0, 0, 0, 0,
 		SWP_NOOWNERZORDER | SWP_NOACTIVATE | SWP_NOMOVE | SWP_NOSIZE
 	);
@@ -670,8 +688,8 @@ void TopWin(struct WLIST *w, struct ARGS
 
 void NotWin(struct WLIST *w, struct ARGS *a)
 {
+	(void)a;
 	SetWindowPos(w->hwnd, HWND_NOTOPMOST, 0, 0, 0, 0,
 		SWP_NOOWNERZORDER | SWP_NOACTIVATE | SWP_NOMOVE | SWP_NOSIZE
 	);
 }
-
